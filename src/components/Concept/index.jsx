import React from 'react';
import './styles.scss';

const Concept = (props) => (
  <div className='concept-container'>
    <h1>{props.title}</h1>
    <h2>Concept container</h2>
  </div>
  );

export default Concept;
